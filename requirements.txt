paho-mqtt>=1.6.1
PyYAML>=6.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
# YOLO-World chạy CPU có kiểm soát thread; DeepStream PGIE/NvDCF giữ GPU.
onnxruntime>=1.22.0,<2

# pyds (DeepStream Python bindings) và PyGObject (gi) cài theo hướng dẫn
# chính thức của NVIDIA DeepStream SDK (không cài qua pip) - dùng bản wheel
# đi kèm SDK, khớp version DeepStream 6.4 / 7.x đang deploy trên server.
